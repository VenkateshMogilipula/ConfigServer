server:
  port: 8082
spring:
  datasource:
    username: postgres
    password: Venky@123
    url: jdbc:postgresql://localhost:5432/courses-db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
